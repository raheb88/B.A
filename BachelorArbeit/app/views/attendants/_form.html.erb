<%= form_for(@attendant) do |f| %>
  <% if @attendant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attendant.errors.count, "error") %> prohibited this candy from being saved:</h2>

      <ul>
      <% @attendant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attendantname %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <!-- :name is from kid -->
    <%= collection_check_boxes(:attendant, :seminar_ids, @seminars, :id, :title) %>
  </div>
  <div class="form-control">
    <div class="col-sm-offset-3 col-sm-5">
      <%= f.submit("Weiter", class: 'btn btn-primary btn-lg') %>
    </div>
  </div>


<% end %>
